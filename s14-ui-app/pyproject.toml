[project]
name = "atlas-ui"
version = "0.1.0"
description = "An agent that may only answer in interfaces (EAG V3 Session 14, Part 2)"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "uvicorn>=0.30", "httpx>=0.27", "pydantic>=2.7"]
